{
  "name": "Week-12",
  "version": "1.0.0",
  "description": "*where \"stuff\" is probably best described as \"streams\"* *yes, I could have just said \"streams\", but it's too late now*",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com-spedu:spedu/Week-12.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "markdown": "^0.5.0"
  }
}
